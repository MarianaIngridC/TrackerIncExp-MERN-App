{
  "name": "back",
  "version": "1.0.0",
  "main": "server",
  "scripts" : {
    "start": "node src/server.js",
    "server": "nodemon src/server.js",
    "client-install": "cd client && yarn install client",
    "client": "yarn run client",
    "dev": "concurrently \"yarn run server\" \"yarn run client\"",
    "heroku-postbuild": "cd client && yarn install && yarn run build"
    
  },
  "license": "MIT",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.10.11",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "nodemon": "^2.0.6"
  }
}
